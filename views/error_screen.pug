doctype html
head
    meta(charset='UTF-8')
    title Error Page
    meta(name='viewport' content='width=device-width, initial-scale=1')
    link(rel='stylesheet' href='https://www.w3schools.com/w3css/4/w3.css')
    style.
        body {
            margin: 0;
            padding: 70px 100px;
            overflow: hidden; /* Disable vertical and horizontal scrolling */
            background-color: #FFFFFF;
            font-family: "Roboto", sans-serif;
        }
            .w3-half {
            max-width: 100%; /* Ensure content fits within the page width */
            margin: 0 auto; /* Center content horizontally if needed */
            padding: 0px; /* Add some padding for spacing */
        }
        .text {
            font-size: 1.5vw;
            padding: 5px;
            margin: 0;
        }
        .errormsg {
            font-size: 2vw;
            font-weight: bold;
            padding: 0;
        }
        button {
            border: none;
            padding: 10px 40px;
            background-color: #cccccc;
            font-weight: bold;
            margin-top: 20px;
        }
        button:hover {
            background-color: #000000;
            color: #FFFFFF
        }

        @media (max-width:600px)
        {
            body {
                margin: 0;
                padding: 30px 70px;
                overflow: hidden; /* Disable vertical and horizontal scrolling */
            }
            button {
                max-height: 10%
                font-size: 1.0vw;
            }
            .text {
                font-size: 2.5vw; /* You can keep the same font size or adjust as needed */
            }
            .errormsg {
                font-size: 3.5vw; /* You can keep the same font size or adjust as needed */
            }
            .w3-half {
                margin-top: 20px;
            }
            .w3-image {
                max-width: 80%;
            }
        }

.w3-row
    .w3-half
        img.w3-image(src='src/error_image.png' alt='error image')
    .w3-half
        p.text Sorry!
        p.errormsg  The page you are looking for does not exist.
        p.text Please check that the website address is spelled correctly.
        p.text Click the button below to return to our home page.
        button  HOME
